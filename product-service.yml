server:
  port: 8083

spring:
  datasource:
    url: jdbc:mysql://localhost:3307/product_db
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true